import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ConfirmarPagarComponent } from './confirmar-pagar.component';

describe('ConfirmarPagarComponent', () => {
  let component: ConfirmarPagarComponent;
  let fixture: ComponentFixture<ConfirmarPagarComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ConfirmarPagarComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ConfirmarPagarComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
