import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DatosSignoComponent } from './datos-signo.component';

describe('DatosSignoComponent', () => {
  let component: DatosSignoComponent;
  let fixture: ComponentFixture<DatosSignoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DatosSignoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DatosSignoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
